'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {evaluate,canTransition}=require('../domain.cjs');const ctx={actor:'customer-1'};const base={customerActorId:'customer-1',restaurantId:'r1',locationId:'l1',serviceable:true,items:[{itemId:'i1',quantity:2,unitPrice:5,available:true,stockAvailable:3,modifiers:['no onion']}],tax:1,tip:2,deliveryFee:3,promotionDiscount:1,total:15,priceListVersion:'menu-v2',taxQuoteRef:'tax:q1',fulfillmentType:'delivery'};test('checkout totals, modifiers and kitchen handoff are deterministic',()=>{const out=evaluate(base,ctx);assert.deepEqual(out,evaluate(base,ctx));assert.equal(out.result.totalCents,1500);assert.equal(out.result.kitchenTicket.modifiersPreserved,true);});test('availability, serviceability, ownership and lifecycle shortcuts fail closed',()=>{const out=evaluate({...base,customerActorId:'other',serviceable:false,total:1,items:[{...base.items[0],stockAvailable:0}]},ctx);assert.ok(out.errors.length>=4);assert.equal(canTransition('draft','delivered'),false);assert.equal(canTransition('confirmed','preparing'),true);});
